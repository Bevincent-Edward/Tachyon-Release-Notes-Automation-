# Core dependencies
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
python-docx==1.1.0

# Pydantic (must use pre-built wheels)
pydantic==2.5.3
pydantic-core==2.14.6

# Utilities
aiofiles==23.2.1
python-dotenv==1.0.0

# AI/LLM clients (compatible versions)
openai==1.12.0
httpx==0.27.0
google-generativeai==0.3.2

# Install with pre-built wheels only
--only-binary :all:
--no-cache-dir
